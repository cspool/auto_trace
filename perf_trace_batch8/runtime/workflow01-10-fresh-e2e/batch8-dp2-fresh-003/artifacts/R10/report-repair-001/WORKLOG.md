# R10 排名时间线展示修订

2026-09-09：用户授权为高延迟/硬件证据及双卡并发、原始利用率、未知间隙添加时间线；项目过多时允许前 20 项/组，分组可选。

- 输入：同一批次已封存 R10 continuation_001 与其 R09 来源；CPU 离线展示修订，不执行新测量，不修改前序产物。
- 保留原始 R10 的完整内嵌数据与完整无损时间线。前 20 仅是可切换的展示选择，完整分母/排名及源记录仍可访问。
- 高延迟按 R09 peer_group_key 分组，组内观测过程时长总和排序（嵌套过程不可跨组加总）；可切换全部组内实例。
- 双卡并发保留设备内 kernel/queue 精确分段；跨设备同轴展示不宣称已验证细粒度同步。
- 原始利用率使用真实点，不平滑、不补零；未知间隙独立排名、阴影标注及来源锚点可查。
- 待完成：生成交互视图、离线浏览器交互/图像检查、独立数据审计、发布完整 Release 并提交发布回执。

第一版 CPU 展示候选完成并通过离线浏览器与独立 R09 CSV 核对。分母：1,237 高延迟 / 12,544 过程，50 个可比较高延迟组，16 个请求/阶段并发组，13 个可用采样过程组，2,491,806 原始点、800 缺口。

用户新增梯形分组要求：组内每条横线保留一个 Process 的真实开始/结束；连接端点形成半透明梯形/折线梯形轮廓。正在将高延迟、并发和原始采样分组主图升级为全组横线，支持逐行展开与点击定位。第一版页面、工具、日志、审计保留在 attempts/001_rectangular_candidate；该候选未作为最终结果发布。

折叠梯形候选已生成：高延迟与可用采样按同类 Process 分组；并发按请求/阶段分组，组内每个 Process 均是一条精确起止横线。起止双轴分别标实际时间，横线只连接身份，长度不代表耗时。可切换单轴、取消折叠、展开每行 36 px 或点击定位实例。折叠阈值为组内过程时长中位数的 2 倍，每个相邻源端点区间压缩至不超过该权重；全部真实起止、间隔、压缩倍数可查。未改变排序或源记录。

候选 002/003 的代码、页面和检查日志也已归档。最终候选 004 正在进行离线浏览器与独立审计；不会把先前候选的通过状态当成当前页面通过。

用户要求去除未追踪的 Request 后半段。已新增裁剪后的精确时间线与图前范围说明：完整客户端包络 744.746845666 s → 保留 192.957683916 s，省略末尾 551.789161750 s。每请求裁剪边界覆盖全部已纳入 Process/kernel/HIP Runtime/layer/forward；源 Request 真实结束时间保留，仅改变显示。逐请求省略时长见 REQUEST_VIEW_SCOPE.json。尾部不能拆分为计算、排队或通信，图中明确说明未追踪/未展示范围。正在进行独立端点审计和离线交互检查。

尾部裁剪已通过独立 R09 上下文端点核对和离线浏览器检查：59,872 个主显示事件及全部细粒度上下文保留；仅 8 个 Request 的显示端点和默认范围改变，完整原始内嵌数据字节不变。精确纳秒跳转、请求定位和重置通过，零外部网络请求。折叠梯形最终概览已确保所有组内横线在概览范围内，展开后每行 36 px。正在封装最终 Release。
