# Workflow / skill forward validation

A separate read-only review evaluated two realistic tasks against the maintained
Workflow05 and R08/R09/R10 skills:

1. Restore retained Batch8 counters and render the two views without profiling.
   The instructions select CPU-only restoration, own-pass resource rates,
   preserved source scope/classification and separate derived outputs; no
   scheduler or fresh completion record is created.
2. Execute a scheduler-assigned fresh Batch8 R10 from valid R09 tables.
   The instructions require the exact fresh prefix, twelve tables, full source
   DP2 identity and a native renderer supporting the current view profile.
   The retained adapter alone cannot establish formal completion.

The review found the target, suggested sequence, visual rules and reference
routing coherent after corrections. Source-profile literals, serial blocks and
active skill hashes were independently checked by the real scheduler parser.
No runtime stage, model or profiler was executed for this documentation check.

The resource audit was also run on the retained 1206 GB/s reference and an
isolated 2400 GB/s test fixture using identical counters/durations. Both passed;
the production reference was unchanged. Empty type selection now fails with
an explicit unsupported-adapter message, without broadening the threshold.
