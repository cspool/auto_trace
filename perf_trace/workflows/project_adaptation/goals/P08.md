# P08 — Generate Selective Non-replay Process-trace Skill

Execute only Workflow-gap Skill Goal P08.

## Objective

Generate `qwen-dcu-workflow05-selective-process-trace` for collecting or
offline-exporting only P07-selected process windows and producing strict
non-replay timing/ownership calibration evidence.

## Authority

Authority file: `perf_trace/workflows/05_workflow04_guided_selective_process_trace_and_resource_gap_analysis.md`.

The complete normative capability contract is the manifest-pinned P08 scope:

- `Workflow 05 执行顺序 / Step 4`;
- the selective measured layer of `表达一`;
- the Selective measured-output subset;
- timing/concurrency Completion Checks and applicable escalation controls.

Only P08 binding evidence may establish the live Qwen hipprof launcher,
process-marker inventory and normalized HIPTX/HIP/HIPOPS schema.

## Required behavior to preserve

The future Skill must consume P07's plan, retain the existing process marker
identity, keep a queryable DB, allow offline PFTrace export, and attribute by
process HIPTX range → contained HIP runtime call/index bounds → HIPOPS with the
same runtime `_Index`. It must output real process/kernel start/end, strict
ownership, busy union, overlap, ambiguity and template calibration. It must not
attribute by GPU timestamp overlap, use replay duration as latency, or expand
to all layer-input occurrences without recorded escalation.

## Unknown bindings

Resolve current-run target-filter/marker support and hipprof PFTrace/range
export behavior at runtime. If the current launcher cannot selectively capture
the plan without changing execution semantics, stop and report the unresolved
binding rather than claiming a selective run.

## Output

Write exactly the declared Skill files and
`perf_trace/workflows/project_adaptation/handoffs/P08.json`. Do not run the
formal trace during Skill generation or modify upstream instrumentation.
