# P09 — Generate Targeted Hardware-gap Skill

Execute only Workflow-gap Skill Goal P09.

## Objective

Generate `qwen-dcu-workflow05-targeted-hardware-gap-analysis` for R4-first
hardware reuse, missing-evidence targeted PMC collection, and long-process
hardware/resource gap analysis.

## Authority

Authority file: `perf_trace/workflows/05_workflow04_guided_selective_process_trace_and_resource_gap_analysis.md`.

The complete normative capability contract is the manifest-pinned P09 scope:

- `Workflow 05 执行顺序 / Step 5`;
- `表达二：长延迟 Process 时间窗分析` analytical requirements;
- targeted hardware and long-process output subset;
- hardware/gap Completion Checks and PMC cost controls.

Only P09 binding evidence may establish current hipprof PMC modes, strict
family joins and DCU diagnostic formulas. Existing R4 Skill remains an upstream
boundary and must not be edited or copied.

## Required behavior to preserve

The future Skill must left-join selected non-replay process/family rows to R4
by the declared identity, classify exact/compatible/exploratory/missing/path
states, and collect PMC/PMC-read/PMC-write only for decision-relevant gaps.
Hardware values may be projected onto P08 non-replay intervals only with
`replay_projected` semantics. Replay duration must never replace latency,
busy union, overlap or critical path. Unavailable fields and path changes must
remain visible. Long-process outputs must combine hardware, runtime, theory
and resource panels without treating projected counters as continuous original
run samples.

## Unknown bindings

Preserve runtime discovery for filtered PMC capture support, current metric
names, gfx936 limits and optional ROCm tools. A failed capability probe is a
stop/degraded evidence state, not permission to substitute another profiler.

## Output

Write exactly the declared Skill files and
`perf_trace/workflows/project_adaptation/handoffs/P09.json`. Do not execute
PMC replay during Skill generation and do not modify R4 products.
