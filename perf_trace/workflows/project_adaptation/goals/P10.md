# P10 — Generate Utilization and Concurrency-analysis Skill

Execute only Workflow-gap Skill Goal P10.

## Objective

Generate `qwen-dcu-workflow05-utilization-concurrency-analysis` for theoretical
workload joins, low-utilization interval construction, dependency/ready/slack
analysis, concurrency opportunities and coverage-driven escalation.

## Authority

Authority file: `perf_trace/workflows/05_workflow04_guided_selective_process_trace_and_resource_gap_analysis.md`.

The complete normative capability contract is the manifest-pinned P10 scope:

- `Workflow 05 执行顺序 / Step 6` and `Step 7`;
- NetworkX and HTA code-reuse boundaries under `可复用工具、接口与代码边界`;
- `表达三：低资源利用率时间窗与可并发机会` analytical requirements;
- theoretical/gap/opportunity outputs, stop controls and concurrency checks.

Only P10 binding evidence may establish current algorithmic-trace, FX process,
runtime/kernel and R4 schemas.

## Required behavior to preserve

The future Skill must compute intervals from real kernel/runtime change points,
keep `observed_in_original_timeline` distinct from `replay_projected`, and
classify idle, launch gap, active underutilization, low concurrency,
resource-limited and unresolved windows. It may call NetworkX only after
auditable dependency evidence exists; temporal adjacency, FX order or same
queue order cannot become tensor dependency. HTA code reuse requires a pinned
commit/license, a project adapter, unit tests and heuristic labels. A
concurrency opportunity requires dependency, ready, slack, runtime/queue
feasibility, resource coexistence and exposed-time impact; otherwise it is
only a candidate. Actual speedup requires a separate experiment.

## Unknown bindings

Keep theoretical bytes/working-set derivation, complete data/control dependency
recovery, HTA adapter source and optional Chakra export runtime-discovered when
the pinned evidence does not prove them. Never invent opaque custom-op internals.

## Output

Write exactly the declared Skill files and
`perf_trace/workflows/project_adaptation/handoffs/P10.json`. Do not execute
profiling or an optimization experiment during Skill generation.
