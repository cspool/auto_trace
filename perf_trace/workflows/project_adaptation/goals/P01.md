# P01：迁移完整 layer-wise 证据 Skill

## Objective

以 `visipruner-same-input-layer-wise-workflow` 的完整目录为唯一规范来源，
生成 `perf_trace/skills/qwen-dcu-same-input-layer-wise-workflow`。最大复用
源文本，只做 Qwen_DCU_Worker_0 项目绑定的文本对齐。

## Workflow role

`perf_trace/workflows/01_layer_wise_end_to_end_trace.md` 只说明该 Skill 为后续
全层 process 估计提供所有 input-layer occurrence 的完整分母证据。Workflow
中的操作文案不是迁移内容。

## Required alignment

- 保持源目录精确文件集合：`SKILL.md`、`agents/openai.yaml`。
- 保留 SAME_INPUT 冻结、layer trace、证据生成、严格归因、完整性审计和
  证据边界的原有方法、顺序与强度。
- 把 VisiPruner、模型、仓库路径和运行环境绑定对齐到 Qwen3.5-27B、
  `pra2026-bh408`、vLLM/PRA、ROCm/DCU/HIP。
- Nsight/CUPTI 只有存在可核验的语义等价绑定时才改写；不能仅按名称替换。
  未确认时保留能力约束并推迟具体工具解析。

不得运行 layer trace、模型、GPU 或 profiler。

## Output

- Skill：`perf_trace/skills/qwen-dcu-same-input-layer-wise-workflow`
- handoff：`perf_trace/workflows/project_adaptation/handoffs/P01.json`

完成前执行 manifest 中 P01 的 final gate。handoff 的 `outputs` 只能包含
上述 Skill 目录。
