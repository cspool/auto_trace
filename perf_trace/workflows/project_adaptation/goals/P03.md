# P03：迁移完整 process performance breakdown Skill

## Objective

以 `visipruner-process-performance-breakdown` 的完整目录为唯一规范来源，
生成 `perf_trace/skills/qwen-dcu-process-performance-breakdown`。最大复用
源文本，只做 Qwen_DCU_Worker_0 项目绑定的文本对齐。

## Workflow role

- `02_representative_fx_process_wise_trace.md`：把本 Skill 放在 P02 handoff
  之后，作为 Stage B consumer。
- `03_process_gpu_hardware_trace.md`：只复用本 Skill 已定义的 process 边界。

这两个角色不改变源 Skill 的规范内容。尤其不得把 Workflow 02 的直接 trace
表述或 Workflow 03 的 NCU/硬件计数采集步骤写进目标 Skill。本控制面没有
得到一个权威硬件 profiling 源 Skill，因此 P03 不负责创造该能力。

## Required alignment

- 保持源目录精确文件集合：`SKILL.md`、`agents/openai.yaml`。
- 保留 portable projection tool、来源、覆盖、layer/component latency、
  FX process attribution、报告形状、默认流程、语义、校验和编辑规则。
- 对齐 Qwen3.5-27B、`pra2026-bh408`、vLLM/PRA 与 ROCm/DCU/HIP 的名称、
  路径、schema 和已确认工具绑定。
- 保留“消费已有 profiling/reconstruction 输出”的原始范围边界，不擅自
  变成运行 profiler 的 Skill。

不得运行 breakdown 脚本、模型、GPU、profiler 或 Workflow。

## Dependency

P02 必须已提交；只把其 handoff 当作合法前序位置，不读取未提交产物。

## Output

- Skill：`perf_trace/skills/qwen-dcu-process-performance-breakdown`
- handoff：`perf_trace/workflows/project_adaptation/handoffs/P03.json`

完成前执行 manifest 中 P03 的 final gate。handoff 的 `outputs` 只能包含
上述 Skill 目录。
