# P11 — Generate Trace Visualization and Reporting Skill

Execute only Workflow-gap Skill Goal P11.

## Objective

Generate `qwen-dcu-workflow05-trace-visualization-reporting` for the linked
Plotly global index, Perfetto observed/overlay timeline, long-process and
low-utilization reports.

## Authority

Authority file: `perf_trace/workflows/05_workflow04_guided_selective_process_trace_and_resource_gap_analysis.md`.

The complete normative capability contract is the manifest-pinned P11 scope:

- direct tool interfaces, hipprof PFTrace, Perfetto and Plotly portions of
  `可复用工具、接口与代码边界`;
- `表达一` in full;
- presentation-only portions of `表达二` and `表达三`;
- trace/tool and human-readable Expected Outputs;
- tool/reuse Completion Checks.

P07–P10 machine outputs are the only analytical inputs. P11 must not create a
second timing, hardware or dependency calculation.

## Required behavior to preserve

The future Skill must use stable keys, keep observed PFTrace immutable, put
estimate/R4/gap/dependency data in a synthetic overlay, and exclude
`projected_composition` slices from observed concurrency/busy/idle/critical
path SQL. Plotly consumes only small CSV/JSON and emits offline HTML. Perfetto
provides the shared time axis, tracks, flow and deep links. Every view must
distinguish observed, inferred, estimate, replay-projected, heuristic and
unavailable evidence. Third-party code reuse must have provenance/license.

## Unknown bindings

Perfetto installation, Trace Processor CLI, TrackEvent generation code,
clock-alignment mechanism, embedding/deep-link base URL, dashboard module
location and HTA adapter commit are runtime discoveries. Missing Perfetto may
allow CSV/Plotly planning output, but cannot justify a custom trace canvas or
claim the complete trace-view deliverable.

## Output

Write exactly the declared Skill files and
`perf_trace/workflows/project_adaptation/handoffs/P11.json`. Do not install
tools, download third-party code, generate runtime dashboards or execute
Workflow 05 during Skill generation.
