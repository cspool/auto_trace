# P02：迁移完整 FX process instrumentation Skill

## Objective

以 `visipruner-fx-process-nvtx-instrumentation` 的完整目录为唯一规范来源，
生成 `perf_trace/skills/qwen-dcu-fx-process-nvtx-instrumentation`。最大复用
源文本，只做 Qwen_DCU_Worker_0 项目绑定的文本对齐。

## Workflow role

`perf_trace/workflows/02_representative_fx_process_wise_trace.md` 只说明本 Skill
是 Stage A producer，其边界 handoff 由 P03 对应的 Stage B consumer 使用。
Workflow 自身的 profiler 步骤不得扩张目标 Skill。

## Required alignment

- 保持源目录精确文件集合：`SKILL.md`、`agents/openai.yaml`。
- 保留运行合同、FX process 到运行时代码映射、range instrumentation、
  handoff、可复现性、range inventory、命名、预期 trace、验证和证据边界。
- 对齐 Qwen3.5-27B、`pra2026-bh408`、vLLM/PRA 与 ROCm/DCU/HIP 的名称、
  路径和已确认工具绑定。
- NVTX/CUDA 概念只有存在可核验等价机制时才改写；未知绑定留到运行时。

不得实现 instrumentation、修改业务源码、运行 FX reconstruction、模型、
GPU 或 profiler。

## Output

- Skill：`perf_trace/skills/qwen-dcu-fx-process-nvtx-instrumentation`
- handoff：`perf_trace/workflows/project_adaptation/handoffs/P02.json`

完成前执行 manifest 中 P02 的 final gate。handoff 的 `outputs` 只能包含
上述 Skill 目录。
