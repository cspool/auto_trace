# P04：迁移完整 segmented process attribution Skill

## Objective

以 `visipruner-segmented-process-attribution` 的完整目录为唯一规范来源，
生成 `perf_trace/skills/qwen-dcu-segmented-process-attribution`。最大复用
源文本，只做 Qwen_DCU_Worker_0 项目绑定的文本对齐。

## Workflow role

`perf_trace/workflows/04_full_layer_fx_process_wise_estimate.md` 只说明该 Skill
消费 P01 的完整 layer 分母和 P03 的代表性 process 能力，在显式 type
mapping 与逐层守恒下产生全层估计。Workflow 文案不覆盖源 Skill。

## Required alignment

- 保持源目录精确文件集合：`SKILL.md`、`agents/openai.yaml`。
- 保留上游证据要求、type map、守恒分配、证据包、验证和解释规则。
- 保留 measured 与 estimated 的边界，禁止把模板缩放呈现为直接测量。
- 对齐 Qwen3.5-27B、`pra2026-bh408`、vLLM/PRA 与 ROCm/DCU/HIP 的名称、
  路径、schema 和已确认工具绑定。

不得运行估计脚本、模型、GPU、profiler 或 Workflow。

## Dependencies

P01 与 P03 必须都已提交；只消费 runner 提供的合法前序 handoff。

## Output

- Skill：`perf_trace/skills/qwen-dcu-segmented-process-attribution`
- handoff：`perf_trace/workflows/project_adaptation/handoffs/P04.json`

完成前执行 manifest 中 P04 的 final gate。handoff 的 `outputs` 只能包含
上述 Skill 目录。
