# P07 — Generate Workflow 05 Evidence-planning Skill

Execute only Workflow-gap Skill Goal P07.

## Objective

Generate `qwen-dcu-workflow05-evidence-planning` for Workflow 05 upstream
evidence validation, tool capability probe, low-cost whole-run index, inferred
kernel ownership, and selective trace/hardware candidate planning.

## Authority

Authority file: `perf_trace/workflows/05_workflow04_guided_selective_process_trace_and_resource_gap_analysis.md`.

The complete normative capability contract is the manifest-pinned Workflow 05
scope for P07:

- `目标` and `核心原则`;
- `Required Inputs`;
- `Workflow 05 执行顺序 / Step 0` through `Step 3`;
- the Workflow-04 reuse and candidate-selection output subset;
- applicable Cost Controls and Completion Checks.

Only P07 `binding_evidence` may establish Qwen/DCU paths, schemas and commands.
The listed precommitted Skills define upstream artifact ownership only.

## Required behavior to preserve

The future Skill must require one compatible SAME_INPUT contract, consume
Workflow 01–04/R4 outputs before new collection, keep measured timing separate
from estimates and inferred owners, perform no-GPU capability checks, build
stable-key whole-run tables, and rank candidates by cumulative latency,
risk/shape distance, ambiguity and hardware coverage. It must emit explicit
`selective_trace_plan.csv` and `selective_hardware_plan.csv`, with reasons and
cost/coverage stop criteria. It must not collect a selective trace or PMC.

## Unknown bindings

Preserve every manifest `unresolved_bindings` entry. Probe it at runtime and
fail or degrade only as allowed by Workflow 05; do not invent a module, path,
schema or tool installation.

## Output

Write exactly:

```text
perf_trace/skills/qwen-dcu-workflow05-evidence-planning/SKILL.md
perf_trace/skills/qwen-dcu-workflow05-evidence-planning/agents/openai.yaml
perf_trace/workflows/project_adaptation/handoffs/P07.json
```

Do not execute Workflow 05, model inference, hipprof collection or PMC replay.
Do not edit existing 01–04 products. Mark complete only when the standalone
Skill and minimal handoff are ready for the external gate.
