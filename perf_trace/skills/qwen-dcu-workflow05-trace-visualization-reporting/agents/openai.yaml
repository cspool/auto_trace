interface:
  display_name: Qwen DCU Process and Resource Views
  short_description: Build a self-contained offline acceptance bundle
  default_prompt: Use $qwen-dcu-workflow05-trace-visualization-reporting to distinguish
    formal fresh R10 from retained presentation, render Process trapezoids and only
    associated hardware-resource windows with simultaneous proportional metrics, and
    audit source scope, omissions and offline operation.
