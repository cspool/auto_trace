# A01：Profiler 与 Patch-target Skill 文本适配

## Objective

消费 A00 迁移 handoff；基于 Unit 01 和
`trace-patch-target-discovery` 参考 Skill，产出当前项目后续可直接使用的
S01 Skill 文本对齐版本。A00 只提供共享合同边界，不向 A01 目标 Skill
注入额外正文。

## Prompt

`01_torch_profile_patch_targets.md` 只用于理解
`trace-patch-target-discovery` 在 A01/R01 中承担 profiler-to-patch-target
发现这一角色；不要把 Workflow 的描述、命令、handoff、gate 或完成检查
写进目标 Skill。

以 `trace-patch-target-discovery` 本身作为唯一、完整且正确的目标约束。
完整保留它的目录结构、资源、章节、方法、步骤顺序、输出格式、验证、
反模式和停止边界，不摘要、不重构、不增删约束。只进行新项目文本对齐：
修改 Skill 名称与触发描述，并把旧项目名称、路径、模型/运行时、符号、
hook、命令、schema、artifact 和示例绑定替换为
`${project_root}/pra2026-bh408` 中经确认的 Qwen3.5、vLLM V1、
ROCm/DCU 对应项。无法在适配阶段确认具体对应项时，保留原要求及其强度，
把具体绑定改成运行时从当前源码、配置和真实执行中发现，不能删除、弱化
或猜测。

产出是参考 Skill 的当前项目文本对齐版本，不是包装它、解释它或重新引用
它的新合同。输出文件及 references/scripts/resources 完全跟随参考 Skill，
不要引入参考 Skill 原本没有的文档层级。A01 只修改文本，不运行 Workflow
或实现业务代码。

## 输入

- A00 已提交的迁移 `handoff.json`；
- `workload_profile/workflows/00_overview.md`
- `workload_profile/workflows/01_torch_profile_patch_targets.md`
- `trace-patch-target-discovery`
- 当前项目基本目录与运行背景
- `workload_profile/workflows/project_adaptation/goals/common.md`

## 必须完成

- 镜像参考 Skill 的完整文件集合；
- 保留其全部 patch-target discovery 方法、格式、验证和反模式；
- 将项目绑定对齐到当前 Qwen3.5/vLLM V1/ROCm/DCU 项目；
- 只在无法确认具体符号时改为运行时发现，不降低原约束；
- 产出只指向目标 Skill 的适配 handoff，供 A02 确认上游已提交；
- 不实现 profiler、wrapper、patch 脚本、verifier 或测试。

## 产出

- `workload_profile/skills/qwen-dcu-profile-patch-targets/`
- `workload_profile/workflows/project_adaptation/artifacts/A01/handoff.json`

完成目标 Skill 镜像和最小 handoff 检查后即可完成。
