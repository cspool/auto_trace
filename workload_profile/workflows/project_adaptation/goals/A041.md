# A041：Dispatch Reconstruction、Tensor Dependency 与 ONNX Skill 文本适配

## Objective

只处理 `03_1_04_1_dispatch_branch.md` 的 04-1 需求。消费 A031 适配
handoff，基于 `dispatch-layer-reconstruct-onnx`，产出 S041 Skill
文本对齐版本。

## Prompt

`03_1_04_1_dispatch_branch.md` 只用于理解
`dispatch-layer-reconstruct-onnx` 在 04-1 中承担 Dispatch evidence
reconstruction 与 ONNX 这一角色；不要把 Workflow 的 handoff、branch
命令、逐事件手工 character visualization、跨事件 completion audit 或
完成记录复制进目标 Skill；这些未覆盖能力由 A051 独立生成。

以 `dispatch-layer-reconstruct-onnx` 的完整 Skill 作为唯一且正确的目标
约束。完整保留它的目录结构、全部资源、逐事件证据读取、
`event_op_index` 覆盖、tensor-id dataflow、process/module 拆分、小形状
Torch flow、逐阶段 ONNX、code review、audit、完成检查和错误边界，不
摘要、不增删、不重组。

只做新项目文本对齐：把 VisiPrune 根目录、模型/stage 名称、固定 shape
示例、visual-adjust/selection/similarity 实例、命令、工具和 artifact
路径，替换为 `${project_root}/pra2026-bh408` 中经确认的
Qwen3.5/vLLM/DCU 对应项。当前项目等价项必须来自 Dispatch rows 与源码；
尚不能确认时保留原规则及验证强度，把具体绑定改成运行时发现，不能删除
op/module/tensor/ONNX 覆盖约束。

产出是该参考 Skill 的当前项目文本对齐版本，完整继承其文件和资源关系，
不复制 Workflow，也不引入统一五-reference 合同。A041 只修改文本，不
运行重建、ONNX 或实现业务代码。

## 输入

- A031 已提交的适配 `handoff.json`；
- `workload_profile/workflows/00_overview.md`
- `workload_profile/workflows/03_1_04_1_dispatch_branch.md` 的 04-1；
- `dispatch-layer-reconstruct-onnx`；
- 当前项目基本目录与运行背景。

## 必须完成

- 镜像参考 Skill 的完整文件集合；
- 保留 reconstruction、tensor dependency、ONNX、可视化、review 和
  audit 的全部方法与强度；
- 将旧项目事件、路径、schema、脚本、命令和 artifact 绑定对齐到当前
  项目；
- 无法确认的具体绑定改为运行时发现，不删除覆盖与审计要求；
- 产出只指向目标 Skill 的适配 handoff；
- 不实现 reconstruction/visualization 脚本、verifier、fixture 或测试。
- 不吸收 A051 的逐事件手工字符图、跨事件集合相等和人工 completion
  record。

## 产出

- `workload_profile/skills/qwen-dcu-dispatch-reconstruct-visualize/`
- `workload_profile/workflows/project_adaptation/artifacts/A041/handoff.json`

完成目标 Skill 镜像和最小 handoff 检查后即可完成。
