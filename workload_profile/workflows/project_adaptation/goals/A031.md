# A031：Dispatch Capture Skill 文本适配

## Objective

只处理 `03_1_04_1_dispatch_branch.md` 的 03-1 需求。消费 A02 适配
handoff，基于 `visipruner-trace-dispatch-profile`，产出 S031 Skill
文本对齐版本。

## Prompt

`03_1_04_1_dispatch_branch.md` 只用于理解
`visipruner-trace-dispatch-profile` 在 03-1 中承担 filtered eager
Dispatch capture/profile validation 这一角色。它不向目标 Skill 提供
内容；不要复制其中的 branch gate、handoff、命令、04-1 描述或审计文本。

以参考 Skill 的 filtered Dispatch 能力段作为唯一、完整且正确的目标
约束。原样保留这一能力段的结构、捕获方法、字段要求、验证、输出、错误
边界和完成条件；不要混入同一参考 Skill 中已由 A02 对齐的 Algorithmic
Trace/selection 能力段，也不要自行简化。

只做当前项目文本对齐：将 VisiPrune 的路径、模型/层角色、命令、脚本、
event schema、CUDA 假设和 artifact 名称，替换为
`${project_root}/pra2026-bh408` 中经确认的 Qwen3.5、vLLM V1、
ROCm/DCU 对应项。TorchDispatch 入口、worker/process、module context、
tensor identity、producer、alias/mutation 等具体绑定若尚不能确认，保留
原约束和验证强度，改写为运行时从当前项目发现。

产出是参考 Skill 该能力段的当前项目文本对齐版本；文件与资源结构继承
参考 Skill，不增加统一合同 references，不引用 Workflow。A031 只修改
文本，不运行 capture 或实现业务代码。

## 输入

- A02 已提交的适配 `handoff.json`；
- `workload_profile/workflows/00_overview.md`
- `workload_profile/workflows/03_1_04_1_dispatch_branch.md` 的 03-1；
- `visipruner-trace-dispatch-profile`；
- 当前项目基本目录与运行背景。

## 必须完成

- 镜像源 Skill 的文件集合，只保留其 filtered Dispatch 能力段；
- 保留该能力段的捕获、profile validation、输出和完成条件；
- 将旧项目 Dispatch hook、事件 schema、脚本、命令和 artifact 绑定对齐
  到当前项目；
- 无法确认的具体绑定改为运行时发现，不混入 Algorithmic Trace/selection
  能力段；
- 产出只指向目标 Skill 的适配 handoff；
- 不实现 capture/profile 脚本、verifier、fixture 或测试。

## 产出

- `workload_profile/skills/qwen-dcu-dispatch-trace/`
- `workload_profile/workflows/project_adaptation/artifacts/A031/handoff.json`

完成目标 Skill 镜像和最小 handoff 检查后即可完成。
