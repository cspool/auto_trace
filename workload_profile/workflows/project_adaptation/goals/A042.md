# A042：FX Process 解释与手工可视化 Skill 文本适配

## Objective

消费 A033 适配 handoff，以 `visipruner-fx-process-visualization` 完整 Skill
为唯一迁移主体，产出当前项目的 FX process 解释与手工可视化 Skill 文本
对齐版本。

目标路径沿用既有兼容名称：

```text
workload_profile/skills/qwen-dcu-fx-reconstruct-visualize/
```

该名称不改变能力边界：A042 消费 reconstruction，不拥有 rule
reconstruction。

## Prompt

`03_2_04_2_fx_branch.md` 只用于理解
`visipruner-fx-process-visualization` 在 rule reconstruction 之后承担逐
process 解释和手工字符可视化的角色。不要把 Workflow 的 reconstruction
命令、final coverage audit 或 completion record 写进目标 Skill。

以 `visipruner-fx-process-visualization` 的完整 Skill 作为唯一且正确的
目标约束。完整保留其目录结构、全部资源、FX node/process 证据读取、中文
逐 process/逐 op 解释、tensor-axis/rectangle/region 字符图、禁止 bulk
renderer、证据边界、process 分类、绘图规则、模板和质量检查，不摘要、
不增删、不重排。

只做当前项目文本对齐：把旧 visual token/span、value-aware selection、
probe/deep check、process 名、路径、shape 和 artifact 示例，替换为
`${project_root}/pra2026-bh408` 中经确认的 Qwen3.5/vLLM/DCU fixed-input
FX 对应项。无法提前确定的 process 或节点模式，保留参考 Skill 的解释
深度、证据强度和绘图质量要求，把具体旧绑定改成运行时依据 A033/R033
reconstruction、FX nodes、shape、dependency 和源码发现。

A042 不生成 reconstruction 规则、process ranges 或 reconstruction
manifest；这些属于 A033/R033。A042 也不执行绘图或业务 Workflow。

## 输入

- A033 已提交的迁移 `handoff.json`；
- `workload_profile/workflows/00_overview.md`；
- `workload_profile/workflows/03_2_04_2_fx_branch.md` 的第 3 节；
- `visipruner-fx-process-visualization`；
- 当前项目基本目录与运行背景。

## 必须完成

- 镜像参考 Skill 的完整文件集合；
- 保留逐事件/process 解释、中文说明、tensor-region 字符图、证据边界和
  质量检查；
- 将旧项目 graph/node、process、shape、脚本、命令和 artifact 绑定对齐
  到当前项目；
- 未确认具体绑定改为运行时从 A033/R033 evidence 发现，不降低解释和视觉
  质量要求；
- 明确拒绝自行重建缺失 process evidence；
- 产出只指向目标 Skill 的最小迁移 handoff；
- 不实现 reconstruction/visualization 脚本、verifier、fixture 或测试。

## 产出

- `workload_profile/skills/qwen-dcu-fx-reconstruct-visualize/`
- `workload_profile/workflows/project_adaptation/artifacts/A042/handoff.json`

完成目标 Skill 镜像和最小 handoff 检查后即可完成。
