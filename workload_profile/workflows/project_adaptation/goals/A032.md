# A032：FX Capture Skill 文本适配

## Objective

只处理 `03_2_04_2_fx_branch.md` 的 03-2 需求。消费 A02 适配 handoff，
基于 `visipruner-fx-trace-workflow`，产出 S032 Skill 文本对齐版本。

## Prompt

`03_2_04_2_fx_branch.md` 只用于理解
`visipruner-fx-trace-workflow` 在 03-2 中承担 selected-layer FX trace
这一角色；不要把 Workflow 的 selection handoff、branch gate、运行命令、
04-2 reconstruction 或 coverage audit 写进目标 Skill。

以 `visipruner-fx-trace-workflow` 的完整 Skill 作为唯一且正确的目标约束。
完整保留其目录结构、`SKILL.md`、bundled `scripts/fx_dynamic_trace.py`
及全部资源和引用关系，以及 eager sampling、wrapper restoration、offline
specialization、`make_fx`、GraphModule、输出、验证、evidence boundary
和 troubleshooting。Workflow 中使用 selected-layer 模式只说明主要触发
场景，不授权删除参考 Skill 的其他内容。

只做项目文本对齐：把 VisiPrune/LLaVA import、模型/层、`DynamicCache`、
visual-selection、CUDA、命令、路径、schema 和 artifact，替换为
`${project_root}/pra2026-bh408` 中经确认的 Qwen3.5、vLLM V1、
ROCm/DCU 对应项。无法在文本阶段确认的 hook、cache/control state、
worker topology、custom op 或 replay 绑定，保留原能力和验证要求，改写为
运行时从当前项目发现。bundled script 及其关系必须保留；A032 不在此阶段
声称已完成代码移植。

产出是参考 Skill 的当前项目文本对齐版本，不复制 Workflow，不增加统一
五-reference 合同。A032 只修改文本，不运行模型/FX 或实现业务代码。

## 输入

- A02 已提交的适配 `handoff.json`；
- `workload_profile/workflows/00_overview.md`
- `workload_profile/workflows/03_2_04_2_fx_branch.md` 的 03-2；
- `visipruner-fx-trace-workflow`；
- 当前项目基本目录与运行背景。

A032 对 A031/A041 没有业务依赖。

## 必须完成

- 镜像参考 Skill 的完整文件集合，包括 bundled
  `scripts/fx_dynamic_trace.py`；
- 保留 eager sampling、specialization、FX capture、验证、证据边界和
  troubleshooting；
- 将旧项目 sampling、guard、cache、脚本、命令和 artifact 绑定对齐到
  当前项目；
- 无法确认的具体绑定改为运行时发现，不声称已经完成脚本代码移植；
- 产出只指向目标 Skill 的适配 handoff；
- 不实现 sampling/trace 脚本、verifier、fixture 或测试。

## 产出

- `workload_profile/skills/qwen-dcu-fx-trace/`
- `workload_profile/workflows/project_adaptation/artifacts/A032/handoff.json`

完成目标 Skill 镜像和最小 handoff 检查后即可完成。
