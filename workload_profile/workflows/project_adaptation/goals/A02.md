# A02：Algorithmic Trace 与 Canonical Selection Skill 文本适配

## Objective

消费 A01 适配 handoff；以 Unit 02 说明环节角色，以
`visipruner-trace-dispatch-profile` 的指定能力段作为迁移主体，产出当前
项目后续可直接使用的 S02 Skill 文本对齐版本。

## Prompt

`02_algorithmic_trace_layer_selection.md` 只用于理解参考 Skill 在本
Workflow 中的作用：`trace-patch-target-discovery` 是 A01 已完成的上游
能力，不是 S02 需要再次合并的迁移主体；S02 对齐
`visipruner-trace-dispatch-profile` 中 Algorithmic Trace 与代表事件选择
这一完整能力段。不要把 Workflow 文本、上游 Skill 内容、handoff、gate、
命令或阶段说明写进目标 Skill。

以该参考 Skill 的 Algorithmic Trace/selection 能力段作为唯一且正确的
目标约束。完整保留这一能力段的结构、方法、步骤、输入输出、验证、错误
边界和完成条件，不摘要、不重新设计，也不混入属于 A031 的 filtered
Dispatch capture 能力段。

只做项目文本对齐：把 VisiPrune/LLaVA 的项目名、路径、模型结构、算法
事件、层角色、命令、脚本、schema、artifact、固定层数和事件数，替换为
`${project_root}/pra2026-bh408` 中经确认的 Qwen3.5、vLLM V1、
ROCm/DCU 对应项。无法预先确认的 hook、事件字段、数量或选择规则，保留
参考 Skill 的原始目标和验证强度，仅将具体绑定改成运行时从当前项目发现。

产出是参考 Skill 该能力段的当前项目文本对齐版本。文件和资源结构继承
参考 Skill，不增加新的合同层，不继续引用旧参考 Skill，也不复制
Workflow。A02 只修改文本，不执行 Algorithmic Trace 或实现业务代码。

## 输入

- A01 已提交的适配 `handoff.json`；
- `workload_profile/workflows/00_overview.md`
- `workload_profile/workflows/02_algorithmic_trace_layer_selection.md`
- Prompt 指定的 `visipruner-trace-dispatch-profile` 源 Skill；
- 当前项目基本目录与运行背景。

## 必须完成

- 镜像源 Skill 的文件集合，只保留其 Algorithmic Trace/selection 能力段；
- 保留该能力段的方法、输出、验证和完成条件；
- 将旧项目 hook、事件、脚本、命令和 artifact 绑定对齐到当前项目；
- 无法确认的具体绑定改为运行时发现，不混入 filtered Dispatch 能力段；
- 产出只指向目标 Skill 的适配 handoff；
- 不实现 trace/selection 脚本、verifier、fixture 或测试。

## 产出

- `workload_profile/skills/qwen-dcu-algorithmic-trace-selection/`
- `workload_profile/workflows/project_adaptation/artifacts/A02/handoff.json`

完成目标 Skill 镜像和最小 handoff 检查后即可完成。
